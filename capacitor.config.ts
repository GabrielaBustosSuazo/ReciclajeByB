import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reciclajebyb.duoc',
  appName: 'ReciclajeByB',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
